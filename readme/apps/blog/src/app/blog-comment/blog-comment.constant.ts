export const DEFAULT_COMMENT_COUNT_LIMIT = 50;

export enum Message {
  PostNotExists = 'Post does not exists',
  PostNotPublished = 'Post does not published',
  CommentNotExist = 'Comment does not exist',
  OtherOwner = 'Comment has other owner'
}
